### Spike Documentation

1. **Change Edit Mode to "Table"**  
   Modify the edit mode property to `"table"` instead of the default `"cell"` to enable table-level editing behavior.

2. **Style Empty Cells on Tab Focus**  
   Implement a visual cue (e.g., background color or outline) for empty cells that get focus when the "Tab" key is pressed, to highlight when navigation between cells occurs.

3. **Remove Borders from Text Input Fields**  
   Remove the default borders for all text input fields. Set the border color to `transparent` to eliminate the visual border while maintaining focus styles.

4. **Add Border for Select Variant Inside Table**  
   Apply a border style to `select` elements within the table to clearly highlight the component for improved visibility and user interaction.

